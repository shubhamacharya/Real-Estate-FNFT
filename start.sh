#!/bin/bash
    cd ./server
    gnome-terminal --tab --title='Node Server' -- npm start
    node = $$
    #gnome-terminal --tab --title='Ganache' -- ganache -m "alert recall gain erupt clerk weekend stem direct chef grocery bone make" -l 3000000000
    #ganache = $$
    #cd ./public/images/NFT
    #gnome-terminal --tab --title='Image Server' -- 'http-server'
    #http = $$

    echo $node
    #echo $ganache
    #echo $http
